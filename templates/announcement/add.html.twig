{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
<main>

        <section class="py-5 text-center container mt-">
            <div class="d-flex py-lg-5">
                <div class="mx-auto">
                    <h1 class="fw-bold text-start">Détail de l'annonce</h1>                    
                </div>
            </div>
             <ul class="navbar-nav me-4 mb-md-0">
            <li class="nav-item">
                <a class="btn btn-primary" aria-current="page" href="{{ url('announcement_browse') }}">Retour</a>
            </li>
        </ul> 
        </section>

               {{ form_start(announcement_form) }}

                
                {{ form_row(announcement_form.title) }}
                {{ form_row(announcement_form.description) }}

                <button type="submit" class="btn btn-primary mt-4">Créer mon annonce</button>

              {{ form_end(announcement_form) }}
                
    </main>
{% endblock %}
