{% extends 'base.html.twig' %}

{% block title %}Espace membre{% endblock %}

{% block body %}

<main>
    <div class="container">
        <div class="d-flex flex-column flex-md-row">
            <div class="d-flex mt-4 text-center mx-auto">
                <h1 class="fw-bold">Bienvenue dans votre espace Membre</h1>
            </div>
        </div> 
        <div class="d-flex flex-column flex-md-row mt-5">
            <div class="mt-5">
                <div class="bg-white mt-3 rounded border profile-heading">
                    <h4 class="px-0 py-2 text-center">Mon compte</h4>
                        <ul class="list-group mx-3">
                            <li class="list-group">
                                <a class="nav-link fs-6" aria-current="page" href="{{ url('profile_member') }}">Infos de connexion</a>
                            </li>
                            <li class="list-group">
                                <a class="nav-link" aria-current="page" href="{{ url('profile_member') }}">Mes coordonnées</a>
                            </li>
                            <li class="list-group">
                                <a class="nav-link" aria-current="page" href="{{ url('profile_member') }}">Mes spécialisations</a>
                            </li>
                            <li class="list-group">
                                <a class="nav-link" aria-current="page" href="{{ url('profile_member') }}">Supprimer mon compte</a>
                            </li>
                        </ul>
                </div>

            </div>
        

            <section class="text-center container mt-4">
                <div class="d-flex">
                    <div class="mx-auto">
                        <h2>Mes paramètres</h2>

                        <div>
                            {# {{ form(member_form) }}       #}
                        </div>

                    </div>
                </div>                  
            </section>
        </div>
    </div>
</main>     
{% endblock %}
